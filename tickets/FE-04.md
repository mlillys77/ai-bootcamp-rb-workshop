# FE-04 — Total story points summary card

GOAL: A summary card above the chart shows total visible story points and item count,
updating with the active filters.

CONTEXT: `src/App.jsx` computes `visible`; a new `src/components/SummaryCard.jsx`
renders the totals.

CONSTRAINTS:
- Totals must be derived from the already-filtered `visible` list (no duplicate filtering).
- Styling consistent with `.chart` card look in `src/styles.css`.

ACCEPTANCE CRITERIA:
- [ ] Card shows correct totals for "all" and for each individual status filter.
- [ ] Component test verifies totals change when the filter changes.

VERIFY WITH: `npm test`
