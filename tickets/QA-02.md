# QA-02 — Edge-case tests for filterByStatus

GOAL: `filterByStatus` is protected by edge-case tests.

CONTEXT: `src/utils/filters.js`; current tests in `tests/filters.test.js` cover
only the happy path.

CONSTRAINTS:
- Tests first. If a test exposes a genuine bug, fix the function minimally and
  note the fix in your summary.

ACCEPTANCE CRITERIA:
- [ ] Empty items array.
- [ ] Status casing mismatch ("Blocked" vs "blocked") — assert and document behavior.
- [ ] Status value that matches no items.
- [ ] Items missing a `status` field entirely.

VERIFY WITH: `npm test`
